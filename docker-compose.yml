# Local development and evaluation stack: five infrastructure containers and the ten applications.
#
# Image tags are selected through IMAGE_TAG. Local development defaults to `local`; CI deployment
# uses service-specific `latest` tags and also publishes immutable sha-* tags for rollback.

name: c17-task-document-platform

x-service-base: &service-base
  build:
    context: .
    dockerfile: backend/infra/Dockerfile
  restart: unless-stopped
  environment: &service-env
    NODE_ENV: ${NODE_ENV:-development}
    LOG_LEVEL: ${LOG_LEVEL:-info}
    JWT_SECRET: ${JWT_SECRET:-dev-secret-min-32-chars-required-000000}
    JWT_TTL_SECONDS: ${JWT_TTL_SECONDS:-1800}
    RABBITMQ_URL: amqp://${RABBITMQ_USER:-c17}:${RABBITMQ_PASSWORD:?set RABBITMQ_PASSWORD}@rabbitmq:5672
    MESSAGING_IN_MEMORY: "false"
    WEB_BASE_URL: ${WEB_BASE_URL:-http://localhost}
  depends_on: &rabbitmq-dependency
    rabbitmq:
      condition: service_healthy
  healthcheck: &app-healthcheck
    test:
      [
        "CMD-SHELL",
        'node -e "fetch(''http://127.0.0.1:'' + process.env.PORT + ''/health'').then((response) => process.exit(response.ok ? 0 : 1)).catch(() => process.exit(1))"',
      ]
    interval: 15s
    timeout: 10s
    retries: 10
    start_period: 30s

services:
  # ---------------------------------------------------------------------------
  # Infrastructure
  # ---------------------------------------------------------------------------

  postgres:
    image: postgres:16.10-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-c17}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:-c17}
    ports:
      - "127.0.0.1:${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      # One database per owning service (V3 §7). Runs only on first initialisation.
      - ./infra/postgres/init-databases.sh:/docker-entrypoint-initdb.d/10-init-databases.sh:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-c17}"]
      interval: 10s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7.4.7-alpine
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "127.0.0.1:${REDIS_PORT:-6379}:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10

  rabbitmq:
    image: rabbitmq:3.13.7-management-alpine
    restart: unless-stopped
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER:-c17}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD:?set RABBITMQ_PASSWORD}
    ports:
      - "127.0.0.1:${RABBITMQ_PORT:-5672}:5672"
      - "127.0.0.1:${RABBITMQ_MANAGEMENT_PORT:-15672}:15672"
    volumes:
      - rabbitmq-data:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 15s
      timeout: 10s
      retries: 10

  minio:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z
    restart: unless-stopped
    command: ["server", "/data", "--console-address", ":9001"]
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY:?set MINIO_ACCESS_KEY}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY:?set MINIO_SECRET_KEY}
    ports:
      - "${MINIO_BIND_ADDRESS:-127.0.0.1}:${MINIO_PORT:-9000}:9000"
      - "${MINIO_BIND_ADDRESS:-127.0.0.1}:${MINIO_CONSOLE_PORT:-9001}:9001"
    volumes:
      - minio-data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 15s
      timeout: 10s
      retries: 10

  clamav:
    image: clamav/clamav:1.4.5
    restart: unless-stopped
    ports:
      - "127.0.0.1:${CLAMAV_PORT:-3310}:3310"
    volumes:
      - clamav-data:/var/lib/clamav
    healthcheck:
      test: ["CMD", "clamdcheck.sh"]
      interval: 30s
      timeout: 20s
      start_period: 300s
      retries: 10

  # ---------------------------------------------------------------------------
  # Applications (V3 §3) — ten independently deployable containers
  # ---------------------------------------------------------------------------

  api-gateway:
    <<: *service-base
    image: ${IMAGE_PREFIX:-c17-task-document-platform}/api-gateway:${IMAGE_TAG:-local}
    build:
      context: .
      dockerfile: backend/infra/Dockerfile
      args:
        APP: api-gateway
    environment:
      <<: *service-env
      PORT: 3000
      AUTH_SERVICE_URL: http://authentication-identity-service:3001
      USER_ROLE_SERVICE_URL: http://user-role-management-service:3002
      TASK_SERVICE_URL: http://task-management-service:3003
      DOCUMENT_SERVICE_URL: http://document-management-service:3004
      DOCUMENT_SECURITY_SERVICE_URL: http://document-security-service:3005
      PERMISSION_SERVICE_URL: http://permission-service:3006
      AUDIT_SERVICE_URL: http://audit-log-service:3007
      NOTIFICATION_SERVICE_URL: http://notification-service:3008
      SECURITY_MONITORING_SERVICE_URL: http://security-monitoring-service:3009
      GATEWAY_TIMEOUT_MS: ${GATEWAY_TIMEOUT_MS:-10000}
      GATEWAY_PREVIEW_TIMEOUT_MS: ${GATEWAY_PREVIEW_TIMEOUT_MS:-300000}
      GATEWAY_UPLOAD_TIMEOUT_MS: ${GATEWAY_UPLOAD_TIMEOUT_MS:-120000}
      STATISTICS_CACHE_TTL_MS: ${STATISTICS_CACHE_TTL_MS:-30000}
      STATISTICS_CACHE_MAX_ENTRIES: ${STATISTICS_CACHE_MAX_ENTRIES:-200}
      CORS_ORIGINS: "${CORS_ORIGINS:-http://13.229.104.126:3100,http://localhost:3001,http://localhost:3100}"
      MINIO_ENDPOINT: ${MINIO_ENDPOINT:-minio}
      MINIO_PORT: ${MINIO_PORT:-9000}
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:-minioadmin}
      MINIO_USE_SSL: ${MINIO_USE_SSL:-false}
    depends_on:
      authentication-identity-service:
        condition: service_healthy
      user-role-management-service:
        condition: service_healthy
      task-management-service:
        condition: service_healthy
      document-management-service:
        condition: service_healthy
      document-security-service:
        condition: service_healthy
      permission-service:
        condition: service_healthy
      audit-log-service:
        condition: service_healthy
      notification-service:
        condition: service_healthy
      security-monitoring-service:
        condition: service_healthy
    ports:
      - "3000:3000"

  authentication-identity-service:
    <<: *service-base
    image: ${IMAGE_PREFIX:-c17-task-document-platform}/authentication-identity-service:${IMAGE_TAG:-local}
    build:
      context: .
      dockerfile: backend/infra/Dockerfile
      args:
        APP: authentication-identity-service
    environment:
      <<: *service-env
      PORT: 3001
      AUTH_DATABASE_URL: postgresql://${POSTGRES_USER:-c17}:${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD}@postgres:5432/auth_db
      REDIS_URL: redis://redis:6379
      USER_ROLE_SERVICE_URL: http://user-role-management-service:3002
      MAIL_HOST: ${MAIL_HOST:-smtp.gmail.com}
      MAIL_PORT: ${MAIL_PORT:-587}
      MAIL_SECURE: ${MAIL_SECURE:-false}
      MAIL_USER: ${MAIL_USER:?set MAIL_USER}
      MAIL_PASS: ${MAIL_PASS:?set MAIL_PASS}
      MAIL_FROM: ${MAIL_FROM:-}
      OTP_TTL_SECONDS: ${OTP_TTL_SECONDS:-600}
      OTP_RESEND_SECONDS: ${OTP_RESEND_SECONDS:-60}
    depends_on:
      <<: *rabbitmq-dependency
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      user-role-management-service:
        condition: service_healthy
    ports:
      - "127.0.0.1:3001:3001"

  user-role-management-service:
    <<: *service-base
    image: ${IMAGE_PREFIX:-c17-task-document-platform}/user-role-management-service:${IMAGE_TAG:-local}
    build:
      context: .
      dockerfile: backend/infra/Dockerfile
      args:
        APP: user-role-management-service
    environment:
      <<: *service-env
      PORT: 3002
      USER_ROLE_DATABASE_URL: postgresql://${POSTGRES_USER:-c17}:${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD}@postgres:5432/user_role_db
    depends_on:
      <<: *rabbitmq-dependency
      postgres:
        condition: service_healthy
    ports:
      - "127.0.0.1:3002:3002"

  task-management-service:
    <<: *service-base
    image: ${IMAGE_PREFIX:-c17-task-document-platform}/task-management-service:${IMAGE_TAG:-local}
    build:
      context: .
      dockerfile: backend/infra/Dockerfile
      args:
        APP: task-management-service
    environment:
      <<: *service-env
      PORT: 3003
      USER_ROLE_SERVICE_URL: http://user-role-management-service:3002
      TASK_DATABASE_URL: postgresql://${POSTGRES_USER:-c17}:${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD}@postgres:5432/task_db
      PERMISSION_SERVICE_URL: http://permission-service:3006
      AUDIT_SERVICE_URL: http://audit-log-service:3007
      PERMISSION_CHECK_TIMEOUT_MS: ${PERMISSION_CHECK_TIMEOUT_MS:-2000}
      AUDIT_TIMEOUT_MS: ${AUDIT_TIMEOUT_MS:-2000}
    depends_on:
      <<: *rabbitmq-dependency
      postgres:
        condition: service_healthy
      user-role-management-service:
        condition: service_healthy
      permission-service:
        condition: service_healthy
      audit-log-service:
        condition: service_healthy
    ports:
      - "127.0.0.1:3003:3003"

  document-management-service:
    <<: *service-base
    image: ${IMAGE_PREFIX:-c17-task-document-platform}/document-management-service:${IMAGE_TAG:-local}
    build:
      context: .
      dockerfile: backend/infra/Dockerfile
      args:
        APP: document-management-service
    environment:
      <<: *service-env
      PORT: 3004
      DOCUMENT_DATABASE_URL: postgresql://${POSTGRES_USER:-c17}:${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD}@postgres:5432/document_db
      PERMISSION_SERVICE_URL: http://permission-service:3006
      AUDIT_SERVICE_URL: http://audit-log-service:3007
      DOCUMENT_SECURITY_URL: http://document-security-service:3005
      TASK_SERVICE_URL: http://task-management-service:3003
      PERMISSION_CHECK_TIMEOUT_MS: ${PERMISSION_CHECK_TIMEOUT_MS:-2000}
      TASK_LOOKUP_TIMEOUT_MS: ${TASK_LOOKUP_TIMEOUT_MS:-2000}
      AUDIT_TIMEOUT_MS: ${AUDIT_TIMEOUT_MS:-2000}
      SECURITY_TIMEOUT_MS: ${SECURITY_TIMEOUT_MS:-5000}
      SECURITY_UPLOAD_TIMEOUT_MS: ${SECURITY_UPLOAD_TIMEOUT_MS:-120000}
      PREVIEW_TIMEOUT_MS: ${PREVIEW_TIMEOUT_MS:-240000}
      DOCUMENT_UPLOAD_MAX_BYTES: ${DOCUMENT_UPLOAD_MAX_BYTES:-5242880}
      PREVIEW_INITIAL_PAGES: ${PREVIEW_INITIAL_PAGES:-10}
      PREVIEW_EXTEND_PAGES: ${PREVIEW_EXTEND_PAGES:-10}
    depends_on:
      <<: *rabbitmq-dependency
      postgres:
        condition: service_healthy
      permission-service:
        condition: service_healthy
      audit-log-service:
        condition: service_healthy
      document-security-service:
        condition: service_healthy
      task-management-service:
        condition: service_healthy
    ports:
      - "127.0.0.1:3004:3004"

  document-security-service:
    <<: *service-base
    image: ${IMAGE_PREFIX:-c17-task-document-platform}/document-security-service:${IMAGE_TAG:-local}
    build:
      context: .
      dockerfile: backend/infra/Dockerfile
      args:
        APP: document-security-service
    environment:
      <<: *service-env
      PORT: 3005
      DOCUMENT_SECURITY_DATABASE_URL: postgresql://${POSTGRES_USER:-c17}:${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD}@postgres:5432/document_security_db
      MINIO_ENDPOINT: minio
      MINIO_PORT: 9000
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:?set MINIO_ACCESS_KEY}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:?set MINIO_SECRET_KEY}
      MINIO_USE_SSL: "false"
      MINIO_BUCKET: ${MINIO_BUCKET:-documents}
      CLAMAV_HOST: clamav
      CLAMAV_PORT: 3310
      CLAMAV_TIMEOUT_MS: ${CLAMAV_TIMEOUT_MS:-10000}
      PREVIEW_RENDER_TIMEOUT_MS: ${PREVIEW_RENDER_TIMEOUT_MS:-180000}
      PREVIEW_MAX_INPUT_BYTES: ${PREVIEW_MAX_INPUT_BYTES:-26214400}
      PREVIEW_MAX_PAGES: ${PREVIEW_MAX_PAGES:-200}
      PREVIEW_INITIAL_PAGES: ${PREVIEW_INITIAL_PAGES:-10}
      PREVIEW_EXTEND_PAGES: ${PREVIEW_EXTEND_PAGES:-10}
      PREVIEW_MAX_DIMENSION: ${PREVIEW_MAX_DIMENSION:-2400}
      DOCUMENT_ACTIVE_KEK_VERSION: ${DOCUMENT_ACTIVE_KEK_VERSION:-1}
      DOCUMENT_KEK_V1: ${DOCUMENT_KEK_V1:-replace-me-local-only}
      DOCUMENT_SIGNATURE_KEY: ${DOCUMENT_SIGNATURE_KEY:-replace-me-local-only}
    depends_on:
      <<: *rabbitmq-dependency
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
      clamav:
        condition: service_healthy
    ports:
      - "127.0.0.1:3005:3005"

  permission-service:
    <<: *service-base
    image: ${IMAGE_PREFIX:-c17-task-document-platform}/permission-service:${IMAGE_TAG:-local}
    build:
      context: .
      dockerfile: backend/infra/Dockerfile
      args:
        APP: permission-service
    environment:
      <<: *service-env
      PORT: 3006
      PERMISSION_DATABASE_URL: postgresql://${POSTGRES_USER:-c17}:${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD}@postgres:5432/permission_db
      TASK_SERVICE_URL: http://task-management-service:3003
      DOCUMENT_SERVICE_URL: http://document-management-service:3004
      TASK_LOOKUP_TIMEOUT_MS: ${TASK_LOOKUP_TIMEOUT_MS:-2000}
      DOCUMENT_ASSOCIATION_TIMEOUT_MS: ${DOCUMENT_ASSOCIATION_TIMEOUT_MS:-2000}
    depends_on:
      <<: *rabbitmq-dependency
      postgres:
        condition: service_healthy
    ports:
      - "127.0.0.1:3006:3006"

  # Runs at exactly one replica. The audit chain has a single writer by design (V3 §5.7.1,
  # ADR-0002); a second replica would fork it. Do not scale this service.
  audit-log-service:
    <<: *service-base
    image: ${IMAGE_PREFIX:-c17-task-document-platform}/audit-log-service:${IMAGE_TAG:-local}
    build:
      context: .
      dockerfile: backend/infra/Dockerfile
      args:
        APP: audit-log-service
    environment:
      <<: *service-env
      PORT: 3007
      AUDIT_DATABASE_URL: postgresql://${POSTGRES_USER:-c17}:${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD}@postgres:5432/audit_db
    depends_on:
      <<: *rabbitmq-dependency
      postgres:
        condition: service_healthy
    ports:
      - "127.0.0.1:3007:3007"
    deploy:
      replicas: 1

  notification-service:
    <<: *service-base
    image: ${IMAGE_PREFIX:-c17-task-document-platform}/notification-service:${IMAGE_TAG:-local}
    build:
      context: .
      dockerfile: backend/infra/Dockerfile
      args:
        APP: notification-service
    environment:
      <<: *service-env
      PORT: 3008
      NOTIFICATION_DATABASE_URL: postgresql://${POSTGRES_USER:-c17}:${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD}@postgres:5432/notification_db
      USER_ROLE_SERVICE_URL: http://user-role-management-service:3002
      MAIL_HOST: ${MAIL_HOST:-smtp.gmail.com}
      MAIL_PORT: ${MAIL_PORT:-587}
      MAIL_SECURE: ${MAIL_SECURE:-false}
      MAIL_USER: ${MAIL_USER:?set MAIL_USER}
      MAIL_PASS: ${MAIL_PASS:?set MAIL_PASS}
      MAIL_FROM: ${MAIL_FROM:-}
    depends_on:
      <<: *rabbitmq-dependency
      postgres:
        condition: service_healthy
    ports:
      - "127.0.0.1:3008:3008"

  security-monitoring-service:
    <<: *service-base
    image: ${IMAGE_PREFIX:-c17-task-document-platform}/security-monitoring-service:${IMAGE_TAG:-local}
    build:
      context: .
      dockerfile: backend/infra/Dockerfile
      args:
        APP: security-monitoring-service
    environment:
      <<: *service-env
      PORT: 3009
      AUTHENTICATION_IDENTITY_SERVICE_URL: http://authentication-identity-service:3001
      SECURITY_MONITORING_DATABASE_URL: postgresql://${POSTGRES_USER:-c17}:${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD}@postgres:5432/security_monitoring_db
    depends_on:
      <<: *rabbitmq-dependency
      postgres:
        condition: service_healthy
    ports:
      - "127.0.0.1:3009:3009"

  web:
    image: ${IMAGE_PREFIX:-c17-task-document-platform}/web:${IMAGE_TAG:-local}
    build:
      context: ./frontend/web
      dockerfile: Dockerfile
      additional_contexts:
        root: .
      args:
        NEXT_PUBLIC_API_BASE_URL: http://api-gateway:3000
    environment:
      NEXT_PUBLIC_API_BASE_URL: http://api-gateway:3000
      NEXT_PUBLIC_APP_ENV: production
    depends_on:
      api-gateway:
        condition: service_healthy
    ports:
      - '80:3000'

volumes:
  postgres-data:
  redis-data:
  rabbitmq-data:
  minio-data:
  clamav-data:
